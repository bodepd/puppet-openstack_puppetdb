This module contains functions that can be used to query
information about remote nodes from puppetdb.

it depends on the puppetlabs-openstack modules and the
repo: github.com/ripienaar/ruby-puppetdb
